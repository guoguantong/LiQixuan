# 开源软件安全分析课程总结报告

**姓名：** 李琪轩
**学号：** 25140931
**日期：** 2026年6月13日
**仓库：** https://github.com/guoguantong/LiQixuan

---

## 一、课程概述

本课程以开源软件安全分析为核心，系统性地涵盖了静态代码分析、Web 应用安全测试、符号执行自动化逆向、以及 AI 驱动的二进制漏洞挖掘四大模块。课程采用理论与实践相结合的教学方式，通过四次递进式实验作业，引导学生从规则驱动的安全扫描出发，逐步深入到 AI Agent 编排的自动化漏洞挖掘，完整覆盖了从源代码到二进制、从 Web 到系统底层的安全分析技术栈。

本报告对课程的四次实验作业进行系统性回顾，阐述各模块的技术原理、实践过程与学习收获。

---

## 二、模块一：Semgrep 静态代码分析

### 2.1 实验概述

第一次实验以 Semgrep 为核心工具，学习如何使用静态分析技术在源代码层面自动发现安全漏洞。实验目标是编写自定义 Semgrep 规则，检测 Python Flask Web 应用中存在的命令注入（Command Injection）漏洞。

### 2.2 Semgrep 技术原理

Semgrep 是一款基于语法树的轻量级静态分析引擎。与传统的正则表达式匹配不同，Semgrep 将源代码解析为抽象语法树，在 AST 层面进行模式匹配，因此能够理解代码的语义结构——例如区分变量名、函数调用和字符串字面量。其核心能力包括：

**模式匹配：** Semgrep 使用类似代码片段的模式语法，能够匹配特定的语法结构。例如 `subprocess.call(..., shell=True, ...)` 可以精确匹配任何使用 `shell=True` 的子进程调用。

**污点追踪：** 这是本次实验使用的核心功能。污点追踪通过定义 Source（不可信数据来源）和 Sink（危险操作汇聚点），自动跟踪数据从源到汇的传播路径。即使数据经过变量赋值、函数传递等中间步骤，只要存在从 Source 到 Sink 的路径，就能被检测到。

**跨文件分析：** Semgrep 能够处理多个文件之间的调用关系，这对于检测跨函数传播的安全问题至关重要。

### 2.3 实验实践

本次实验编写了一条自定义规则 `Liqixuan-command-injection.yaml`：

```yaml
rules:
- id: test_rule
  message: command injection
  severity: ERROR
  languages: [python]
  mode: taint
  pattern-sources:
    - pattern: request.form.get(...)
    - pattern: request.args.get(...)
  pattern-sinks:
    - pattern: subprocess.$FUNC(..., shell=True, ...)
      metavariable-regex:
        metavariable: $FUNC
        regex: "^(check_output|run|call|Popen)$"
```

该规则定义了 Source 为 Flask 的 HTTP 请求参数（`request.form.get` 和 `request.args.get`），Sink 为使用 `shell=True` 的子进程调用函数（`subprocess.check_output`、`subprocess.run`、`subprocess.call`、`subprocess.Popen`）。当用户可控的数据未经适当过滤直接拼接到 shell 命令中时，就构成命令注入漏洞。

同时编写了测试文件 `Liqixuan-command-injection.test.py`，包含正例和反例，用于验证规则的准确性和召回率。

### 2.4 实验成果

对目标 Flask 应用 code-repo 进行扫描，成功发现 2 个真实的命令注入漏洞：

1. **文件下载接口：** `subprocess.call(["sh", "-c", "cat " + filepath], shell=True)` — `filepath` 参数来自用户输入，通过 `os.path.join` 处理后直接拼接到 shell 命令中，存在命令注入和路径遍历双重风险。

2. **Ping 诊断接口：** `subprocess.check_output("ping -c 1 " + host, shell=True, ...)` — `host` 参数来自 POST 表单，直接拼接进 shell 命令，攻击者可注入任意命令。

两类漏洞均被标记为 Blocking 级别，体现了 Semgrep 在实际安全审计中的有效性。

---

## 三、模块二：Web 应用安全渗透测试

### 3.1 实验概述

第二次实验将视角转向实际的 Web 应用安全测试。以教育部教育漏洞报告平台为依托，对北京交通大学教学支撑平台开展黑盒渗透测试，遵循《网络安全法》和教育平台的各项规定，仅进行读取和探测操作，不修改任何数据。

### 3.2 测试方法论

本次测试采用标准的信息收集—漏洞探测—验证报告的流程：

**信息收集阶段：** 通过 HTTP 响应头分析确认服务器为 nginx，后端框架为 Django Python；识别出开放页面为登录页面；通过 CSS 版本号 `base.css?v=2.6.38` 确认系统版本信息。

**漏洞探测阶段：** 对登录接口进行速率限制测试，通过连续发送 POST 请求观察服务器限流行为；分析登录表单的 CSRF Token 机制和跳转参数的过滤程度；检查 HTTP 安全响应头的配置情况。

**验证与报告阶段：** 为每个漏洞编写可直接运行的 POC 代码，提供至少 3 张截图进行辅助说明，给出具体可行的修复建议。

### 3.3 发现的漏洞

本次测试共发现 5 个安全漏洞，其中包括 2 个中危漏洞和 3 个低危漏洞：

**BJTU-2026-001：登录接口弱速率限制（中危，6/20）**

登录接口虽存在 nginx 层面的 IP 速率限制（约 7-8 次/30 秒后返回 503），但该限制过于宽松，且缺少验证码机制与账号锁定策略。攻击者每 30 秒可尝试 6 次，即每分钟 12 次，每小时 720 次。对于常见弱密码，仅需约 83 分钟即可完成字典攻击。POC 脚本采用批次冷却策略（每批 6 次请求 + 30 秒等待），成功绕过了 nginx 限流。

**BJTU-2026-002：开放重定向漏洞（中危，6/20）**

登录页面的 `next` 参数用于登录成功后的跳转，但未对目标 URL 进行白名单验证，接受任意外部 URL 及 `javascript:` 伪协议。攻击者可构造恶意链接诱导用户点击后跳转到钓鱼页面。

**BJTU-2026-003~005：安全配置缺陷（低危，2-4/20）**

包括缺少 X-Frame-Options 和 Content-Security-Policy 等关键安全 HTTP 响应头、Cookie 缺少 Secure 标志、服务器版本信息泄露等配置问题。虽然单项风险较低，但多项配置缺陷叠加会显著增加系统整体攻击面。

### 3.4 学习收获

本次实验让学生以教育部漏洞报告平台渗透测试工程师的角色身份，完整经历了从信息收集到漏洞报告的专业安全测试流程。学生不仅掌握了速率限制测试、重定向验证、HTTP 头审计等具体测试技术，更深刻理解了安全测试的伦理边界——只读取不修改、发现高危立即停止并报告、不泄露敏感信息等职业规范。

---

## 四、模块三：ReAct Agent + angr 符号执行

### 4.1 实验概述

第三次实验引入 AI Agent 概念，要求学生实现基于 ReAct 范式的智能分析系统。Agent 使用 LLM（大语言模型）作为编排层，angr（符号执行框架）作为可调用工具，对二进制 crackme 程序进行自动化逆向分析，通过符号执行求解正确密码。

### 4.2 ReAct 范式

ReAct 是一种将推理与行动交替进行的 Agent 架构，其核心循环为：

```
Thought → Action → Observation
   ↑                      ↓
   └──────────────────────┘
```

**Thought（思考）：** LLM 根据当前状态和目标，推理下一步应该采取什么行动。

**Action（行动）：** Agent 调用具体的工具函数执行操作，如加载二进制文件、反汇编函数、执行符号执行等。

**Observation（观察）：** 工具返回的客观结果。这是 ReAct 范式最关键的设计原则——Observation 仅来自工具的实际返回值，不包含 LLM 的臆测或编造。

### 4.3 工具设计

实验使用 LangChain 的 Tool Calling 协议封装了三个 angr 工具：

**analyze_binary：** 加载 ELF 二进制文件，运行 CFGFast 分析，通过符号表定位 `check_password`（密码验证函数）和 `gadget_trap`（死循环陷阱函数）的地址。

**locate_success_address：** 反汇编 `check_password` 函数，自动定位 Success 路径对应的基本块地址作为符号执行的 find 目标，`gadget_trap` 地址作为 avoid 目标。

**symbolic_explore_and_extract：** 使用 angr SimulationManager.explore 执行符号执行，通过 blank_state 从 `check_password` 入口开始，将符号输入通过 RDI 传递，约束为可打印 ASCII 字符，探索器通过 find/avoid 避开陷阱路径、到达 Success 路径，最后通过 z3 约束求解器反推具体密码。

### 4.4 技术原理

**符号执行：** 符号执行的核心思想是用符号变量替代具体的程序输入值，在程序执行过程中维护路径约束条件，最后通过约束求解器（z3）求出满足特定路径条件的输入值。与传统模糊测试的随机尝试不同，符号执行能够精确求解复杂的数学约束（如 `(input[2] ^ 0x12) == 'q'`）。

**blank_state 技术：** 实验中不从程序入口点开始执行（这会导致在 libc 函数如 strlen、scanf 中遭遇路径爆炸），而是使用 `blank_state` 直接从 `check_password` 函数入口开始，手动设置 RDI 寄存器指向符号输入缓冲区，有效将探索范围从整个程序缩小到几十个基本块。

**LLM 编排：** LLM 理解 crackme 的语义结构（密码检查逻辑、陷阱路径），据此指定 find/avoid 地址，将探索范围从数万个基本块缩小到几十个。此外，LLM 能根据观察结果调整策略（如重新选择 find 地址），形成闭环优化。

### 4.5 实验成果

Agent 在 3 轮 ReAct 交互中成功求解出密码 "AZcE"（约束推导：input[0]='A'，input[1]='Z'，input[2]=chr('q'^0x12)='c'，input[3]=chr('H'-3)='E'）。实际运行 crackme 验证了密码的正确性，并验证了四条路径（Success、Trap、Wrong password、长度不足）的完整覆盖。

---

## 五、模块四：ReAct Agent + r2 + Ghidra 二进制静态漏洞挖掘

### 5.1 实验概述

第四次实验是课程的综合作业，要求将 ReAct Agent 范式应用于真实的安全漏洞挖掘场景。与上一次实验的目标不同——上一次是求解特定密码，本次是在完全无源码、无符号表的黑盒条件下，对未知 ELF 二进制文件进行静态漏洞挖掘。工具从 angr 替换为 radare2 和 Ghidra 这两个逆向工程领域的工业标准工具。

### 5.2 实验要求

目标文件 `targets/challenge` 是一个 Linux x86_64 ELF 二进制文件，已执行 strip 去除符号表，无任何源码。分析方式限于纯静态分析，不进行动态验证或 exploit 开发。Agent 必须输出结构化漏洞结论 `vuln.json`，包含三个固定字段：

- `vuln_type`：漏洞类型（如 stack_buffer_overflow）
- `location`：Sink 所在函数或地址
- `cause`：一句话说明不可信输入如何到达危险操作

这是老师特别强调的评分标准——必须有固定字段才算合格。

### 5.3 工具设计与封装

实验封装了 5 个工具（r2 三件套 + Ghidra 两件套）：

**r2_analyze_binary：** 加载二进制文件，执行 `aaa` 完整分析，通过 `afl` 列出所有函数，通过 `ie` 获取入口点信息并反汇编入口点代码。这是分析的第一步，用于获取程序结构的全局视图。

**r2_disassemble_function：** 反汇编指定的函数（通过地址或函数名），同时搜索二进制中的危险函数调用（strcpy、gets、sprintf、scanf、fgets 等）。这是深入分析可疑函数的主要工具。

**r2_analyze_xrefs_and_sinks：** 执行交叉引用分析，追踪危险 Sink 函数被哪些调用者引用。例如找到 `__strcpy_chk` 被 main 函数调用，并分析 Source（fgets 读取的用户输入）到 Sink 的完整数据流路径。

**ghidra_decompile_function：** 使用 Ghidra Headless 反编译器将指定函数反编译为伪 C 代码。与汇编代码相比，反编译输出更接近源代码的语义层次，有助于快速理解程序的业务逻辑。

**ghidra_analyze_binary：** 对整个二进制进行完整的 Ghidra 分析，反编译所有识别出的函数，并自动生成安全发现摘要。

### 5.4 漏洞分析过程

Agent 的六轮 ReAct 分析完整展示了从信息收集到漏洞确认的分析闭环：

**第 1 轮（r2 概览）：** 加载二进制，执行 aaa 分析，获取 10 个函数的列表和入口点信息。二进制被识别为动态链接的 64 位 ELF，由 GCC 11.4.0 编译，已 strip 去符号。

**第 2 轮（r2 深度分析）：** 反汇编 main 函数（0x401264），这是代码量最大（131 字节）的函数。在反汇编输出中发现了关键的数据流：`fgets(128)` 读取用户输入 → `strlen` 长度检查（≤100）→ `__strcpy_chk` 复制到栈。特别注意到 `__strcpy_chk` 的第三参数 `edx=0x10`（16），而栈帧分配为 `sub rsp, 0xa0`（160 字节），存在严重的参数不匹配。

**第 3 轮（Ghidra 语义分析）：** 反编译 main 函数为伪 C 代码，从语义层面确认了数据流：`fgets(input, 128, stdin)` 是 Source，`__strcpy_chk(buf, input, 16)` 是 Sink。其中 `buf` 实际有 160 字节但声称只有 16 字节。

**第 4 轮（排除误报）：** 反编译辅助函数 `helper_format_print`（0x401216），确认它使用 `__snprintf_chk` 并正确传入了 dest_size=160，安全可靠，从而排除了对其他函数的怀疑。

**第 5 轮（数据流验证）：** r2 交叉引用分析确认了完整的 Source-to-Sink 数据流路径，并提取了所有字符串（如 `[%s] %s`、`profile-service ready`、`boot`、`selftest`），这些字符串进一步确认了程序的业务逻辑为一个简单的 profile-service。

**第 6 轮（综合确认）：** Ghidra 完整分析作为最终验证，对全程序进行全面反编译和漏洞标记。

### 5.5 漏洞结论

最终确认的漏洞为栈缓冲区溢出（stack_buffer_overflow）：

- **Sink 位置：** main 函数偏移 0x401382 处的 `__strcpy_chk` 调用
- **数据流：** stdin → fgets(128) → 用户输入缓冲区（rsp+0x20）→ strcspn 去换行符 → strlen 长度检查（≤100）→ `__strcpy_chk(dst=rsp, src=input, 16)`
- **核心矛盾：** 长度检查允许最多 100 字节通过，但 `__strcpy_chk` 的 dest_size 参数仅为 16 字节——而实际栈帧有 160 字节。这种 mismatch 导致两种后果：(1) 若 FORTIFY_SOURCE 禁用，则发生实际的栈溢出，可覆盖返回地址；(2) 若 FORTIFY_SOURCE 启用，则 `__chk_fail` 触发程序异常终止（DoS）。

### 5.6 可运行性设计

Agent 源码设计了双模式运行能力：当系统安装了 r2 和 Ghidra 时，使用 subprocess 直接调用真实工具，所有 Observation 来自工具的真实返回；当工具不可用时，自动切换到 demo 模式，使用基于 capstone + pyelftools 预计算的分析结果展示完整 ReAct 过程。这保证了代码在任何环境下都可以一键运行，不会因为缺少依赖工具而无法验证。

---

## 六、课程知识体系总结

### 6.1 技术栈覆盖

本课程的四次实验从不同层次覆盖了软件安全分析的核心技术栈：

**源代码层面（模块一）：** Semgrep 的污点追踪（Taint Analysis）技术展示了如何在 AST 层面进行跨函数的危险数据流检测。学生理解了 Source-Sink 模型，掌握了自定义安全规则的能力。

**Web 应用层面（模块二）：** 黑盒渗透测试涵盖了认证安全、速率限制、重定向安全、HTTP 安全头、Cookie 安全、信息泄露等 Web 安全的经典议题。学生体验了真实的漏洞报告平台流程。

**二进制逆向层面（模块三、四）：** 从 angr 符号执行到 r2 反汇编再到 Ghidra 反编译，构成了完整的二进制分析技术链。符号执行展示了约束求解的精确性，r2 提供了汇编级别的控制流洞察，Ghidra 反编译则将分析提升到了语义层面。

**AI 编排层面（模块三、四）：** ReAct Agent 是课程的核心创新点。传统的安全工具需要安全专家人工操作和解读结果，而 ReAct Agent 将 LLM 作为决策与编排层，实现了工具调用的自动化和分析策略的智能化。LLM 理解二进制语义、识别危险模式、排除误报、调整策略——这些原本需要人工经验才能完成的任务，现在由 AI 在数十秒内自主完成。

### 6.2 工具链全景

课程中涉及的安全工具生态系统：

| 类别 | 工具 | 应用场景 |
|------|------|----------|
| 静态代码分析 | Semgrep | 源代码漏洞自动发现 |
| Web 安全测试 | Burp Suite / curl / requests | HTTP 请求分析与漏洞验证 |
| 二进制分析 | radare2 (r2) | 反汇编、控制流分析、交叉引用 |
| 反编译器 | Ghidra | 二进制到伪 C 代码转换 |
| 符号执行 | angr + z3 | 路径探索与约束求解 |
| AI 编排 | LangChain + LLM | 工具调用编排、语义分析、策略决策 |
| 二进制解析 | pyelftools + capstone | ELF 结构解析、指令级反汇编 |

### 6.3 能力培养

通过四次实验的递进式训练，学生获得了以下能力：

**安全规则设计能力：** 能够分析漏洞模式，设计精确的匹配规则或污点追踪规则，并编写测试用例验证规则效果。

**渗透测试能力：** 掌握了信息收集→漏洞探测→POC 编写→报告输出的完整渗透测试流程，理解了安全测试的伦理边界和法律要求。

**逆向工程能力：** 能够使用 r2 和 Ghidra 对无源码的二进制文件进行分析，从汇编代码和反编译结果中理解程序逻辑，识别安全缺陷。

**AI Agent 设计能力：** 理解了 ReAct 范式的设计原理，能够将安全工具封装为 LLM 可调用的 Tool，设计 Thought→Action→Observation 闭环，实现自动化的安全分析流程。

**数据流分析思维：** 无论是在源代码层面追踪请求参数到子进程调用的传播路径，还是在二进制层面追踪 fgets 输入到 strcpy 的复制路径，其核心都是 Source-to-Sink 的数据流分析思维——这一思维贯穿了所有四个实验模块。

---

## 七、反思与展望

### 7.1 实验设计的递进性

四次实验的设计体现了从简单到复杂、从人工到自动化的清晰递进路径：模块一使用现成的 Semgrep 规则框架，学生只需编写规则；模块二要求学生独立完成完整的渗透测试流程；模块三引入 AI Agent，但 angr 工具已预定义好，学生聚焦于 Agent 循环的实现；模块四融合了最多的难点——学生需要理解两个工业级工具（r2、Ghidra）的接口，实现完整的 Tool Calling 集成，并针对真实未知二进制生成结构化漏洞结论。这种递进设计使学习曲线平滑上升，每一步构建在上一步的能力基础之上。

### 7.2 AI 在安全分析中的角色

课程最核心的启示在于 AI 在安全分析中的定位。LLM 不是安全的万灵药——它不能直接扫描漏洞，也不能执行符号执行。但 LLM 卓越的语义理解能力和常识推理能力，使其成为连接工具与目标的理想编排层。就像实验中展示的：LLM 从 10 个函数中识别出 main 是需要重点分析的函数，从反汇编输出中注意到 dest_size 与栈帧大小的矛盾，从 Ghidra 反编译结果中排除安全的辅助函数——这些决策和判断，传统上需要资深逆向工程师的经验和直觉。

### 7.3 未来方向

基于本课程的学习，可以展望几个有前景的扩展方向：

一是增加动态分析维度。当前实验限于静态分析，若将 GDB 或 QEMU 封装为可调用工具，Agent 可以在静态分析发现可疑点后主动触发动态验证，形成「静态筛查 + 动态确认」的闭环。

二是引入漏洞可利用性评估。当前 Agent 仅判断漏洞存在与否，若能自动计算缓冲区大小与返回地址之间的偏移，并结合 checksec 检测的安全缓解措施（如 Stack Canary、PIE、NX），Agent 可以给出漏洞的可利用性评分。

三是多 Agent 协作分析。可以将代码审计 Agent、二进制分析 Agent 和渗透测试 Agent 组成协作团队，由中央编排 Agent 统一调度，实现从源代码到运行时到网络层的全维度安全评估。

---

## 八、结语

本课程通过四次精心设计的实验，系统性地覆盖了开源软件安全分析从源代码到二进制、从 Web 到系统底层、从规则驱动到 AI 编排的完整知识体系。从学习使用 Semgrep 编写第一条安全规则，到构建能自主分析未知二进制漏洞的 ReAct Agent，每一次实验都在前一次的基础上推动学生向更高的自动化分析能力迈进。

在 AI 技术快速发展的今天，安全分析的方法论正在经历深刻变革。传统的人工代码审计逐行检查，有效率低、覆盖面窄的固有问题；纯工具的自动扫描虽然快速，但产生大量误报、缺乏上下文理解。本课程所探索的「AI 编排 + 专业工具」的架构范式，为安全分析的智能化转型提供了一个清晰且可实践的路径。LLM 提供决策智能和语义理解，专业工具提供精确的技术操作能力——两者的结合，正在定义下一代安全分析的工作方式。

---

*本报告涵盖了 2026 年 4 月至 6 月期间「开源软件安全分析」课程的全部实验内容。*
