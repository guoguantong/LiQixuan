# 开源软件实践与安全分析 — 课程总结报告

**姓名：** 李琪轩
**学号：** 25140931
**日期：** 2026年6月13日
**授课教师：** 李强
**代码仓库：** https://github.com/guoguantong/LiQixuan

---

## 一、课程概述

「开源软件实践与安全分析」由北京交通大学李强老师讲授，是一门面向开源软件生态、源代码安全分析、二进制逆向工程以及 AI 驱动自动化分析的综合性课程。课程设计了六大教学模块：开源软件生态、源代码层面的软件分析与安全、二进制层面的软件分析与安全、研究性专题教学、小组汇报、课程报告。评分体系为课堂表现占 15%、小组汇报占 35%、课程报告占 50%，体现了过程评价与深度研究并重的教学理念。

课程的核心方法论是：以开源工具链为基础设施，以大语言模型为编排中枢，将传统安全分析中依赖人工经验的知识——漏洞模式识别、函数调用链追踪、约束条件求解——编码为可自动化执行的工具和规则，最终实现对未知软件系统从源代码到二进制层面的全维度安全分析。

本报告按照课程教学模块的顺序，对各章节的理论知识、实验实践与学习收获进行系统性回顾。

---

## 二、开源软件生态与 Git 协作

### 2.1 教学内容

课程以开源软件生态的宏观图景开篇。李强老师在导论中阐述了开源软件的两层含义：软件形态上，源代码可获取并按许可证使用；协作方法上，开放、透明、社区参与、持续演进。从 1970 年代软件被视为硬件附属、1983 年 Richard Stallman 发起 GNU 计划、到 1998 年「Open Source」术语的确立，开源运动经历了从理想主义到工业主流的完整演变。

在现代软件工程中，开源组件已占据代码库的绝对多数。DevOps 工具链（构建、测试、部署）大量依赖开源基础设施；云原生技术栈（语言运行时、框架、中间件、容器编排）几乎全部建立在开源项目之上；甚至生成式 AI 的许多基础模型也以开源或开放权重形式发布。理解开源生态不仅是技术问题，更是理解当代软件产业运作方式的必修课。

### 2.2 Git 核心概念与实用主义操作

Git 是开源协作的基础设施。课程讲义从两个维度组织 Git 教学：实用主义（覆盖 90% 日常需求的核心命令）和深入探索（分支管理、历史操作、问题排查）。

**数据模型：** Git 是一个内容寻址的文件系统，每次 commit 生成唯一 SHA-1 哈希标识，commit 对象包含指向树对象（tree）、父提交（parent）的引用和元数据。理解这一底层模型——工作区（Working Directory）、暂存区（Stage）、本地版本库（Local Repository）、远程版本库（Remote Repository）四层架构——是掌握所有高级操作的前提。

**分支模型：** Git 的分支是极其轻量的可变指针（仅 41 字节的引用文件），这使得创建和切换分支几乎没有成本。课程重点讲授了 HEAD 指针的概念、Fast-forward 合并与三路合并（3-way merge）的区别以及冲突的产生与解决机制。

**实用命令链：** `git clone` → `git config` → `git branch` → `git checkout` → `git status` → `git add` → `git commit` → `git push` → `git pull` → `git log` 构成日常协作的完整闭环。深入命令如 `git merge`、`git rebase`、`git cherry-pick`、`git reset`、`git stash`、`git reflog` 等为复杂场景提供了精细的控制能力。

### 2.3 Git 课堂实验

实验要求学生独立完成一个从本地初始化到远程推送的完整 Git 工作流：

1. 初始化本地仓库，创建 `README.md` 包含姓名、学号、日期，提交。
2. 继续在 `README.md` 上补充两段不同主题的内容，拆分为至少两次独立提交（训练原子化提交习惯）。
3. 创建并切换到以学号命名的功能分支（如 `notes-22221234`），在新分支上新建 `notes.md`，写三个小标题并提交，再做至少两次后续修改分别提交，使功能分支合计至少三次提交。
4. 切回主线，完成一次与 `notes.md` 无关的修改并提交（目的是让后续合并更容易出现非快进合并）。
5. 在主线执行 `git merge` 合并功能分支，若出现冲突则手动编辑去掉冲突标记 `<<<<<<<`、`=======`、`>>>>>>>`，保留合理内容后完成合并。
6. 关联 GitHub 远程仓库，推送主线和功能分支。
7. 输出 `git log --oneline --decorate --graph -n 25` 作为实验证明。

这一实验覆盖了 Git 协作的全部核心环节：初始化、分支隔离、独立开发、冲突解决、合并集成的完整流程。关键的教学意图在于培养「分支隔离、渐进集成」的工程思维——这不仅是 Git 的操作技巧，更是开源社区协作的基本范式。

---

## 三、PyPI 与 Python 打包

### 3.1 教学内容

课程以 PyPI（Python Package Index）为切入点讲授开源软件的分发与供应链安全。Python 打包体系看似繁杂——`setuptools`、`wheel`、`sdist`、`pyproject.toml`、`pip`、`twine`、`TestPyPI`、`build`——但课程从「谁在什么环境运行 → 选哪种分发方式」的决策树出发，帮助学生建立清晰的概念地图。

**分发格式：** `sdist`（源码分发）是压缩归档（`.tar.gz`），包含纯 Python 代码，适合目标环境能自行构建的场景。`wheel`（二进制分发）是预构建的安装包，安装速度更快，`pip` 总是优先选择 wheel。官方推荐同时发布两种格式。

**构建体系现代化：** 课程强调以 `pyproject.toml` 为中心的现代构建配置，`setup.py` 被视为遗留路径。构建前端（如 `python -m build`）调用后端（setuptools/flit/hatch）生成 `dist/` 下的制品。

**安全与供应链：** API token 是发布凭证，必须通过环境变量注入而不能写进仓库。课程特别提到了依赖混淆攻击（Dependency Confusion）——攻击者在公开索引上注册与内部包同名的恶意包——以及 SBOM（软件物料清单）和依赖扫描等供应链安全议题。国内课堂环境下的镜像使用（如清华 TUNA 镜像）也作为实践要点被强调。

### 3.2 PyPI 实验

实验要求学生在 TestPyPI（练习环境，与正式 PyPI 隔离）上完成最小编译发布流程：

1. 在 TestPyPI 注册账号。
2. 准备一个最简可安装项目（含 `pyproject.toml`），按官方教程打包。
3. 创建 API token，使用 `twine upload --repository testpypi dist/*` 上传，确保 token 不写入仓库。
4. 使用 `pip install -i https://test.pypi.org/simple/ ...` 验证安装。

这一实验让学生亲历了从代码编写到包发布再到安装验证的完整软件分发链路——这是开源软件开发的基础能力。

---

## 四、源代码层面的软件分析与安全

### 4.1 LLM for Software Engineering

课程用一整章讲义（70 页）系统梳理了大语言模型在软件工程中的应用谱系。教师将 LLM4SE 领域按模型架构分为三大类：

**Encoder-only 模型（如 BERT/CodeBERT/GraphCodeBERT）：** 通过掩码或对比预训练产生语义向量，接分类头完成缺陷检测、代码相似度计算、代码搜索等判别任务。

**Encoder-Decoder 模型（如 T5/CodeT5/PLBART）：** 适合序列到序列任务——代码翻译、注释生成、条件重写、从规约到草码。Edit 建模样式与 span denoising 是核心训练方法。

**Decoder-only 模型（如 GPT 系列/CodeGen/StarCoder/Code Llama）：** 具备续写、补全、对话、工具调用等强因果建模能力。指令微调（Instruction Tuning）、RLHF、Fill-in-the-Middle 训练使其适用于广泛的代码生成和交互场景。

课程还介绍了大模型在具体软件工程任务中的研究前沿：需求歧义检测（Ambiguity Detection）、类图派生（Class Diagram Derivation）、GUI 布局生成、缺陷检测与程序修复等。

### 4.2 Semgrep 教学与实验

Semgrep 是课程在源代码安全分析层面选用的核心工具。它是一种基于 AST 模式匹配的轻量级静态分析引擎，用 YAML 描述代码问题模式，在项目中进行自动扫描。

**规则体系：** 从最简单的 `pattern: eval(...)` 出发，课程系统讲授了省略号 `...`（匹配任意中间内容）、元变量 `$X`（捕获子表达式用于约束或输出）、`patterns`（多条件 AND 组合）、`pattern-either`（多分支 OR 组合）、`pattern-not`（排除误报）等语法。进阶部分深入污点追踪（Taint Analysis）：定义 Source（如 `request.form.get(...)`）和 Sink（如 `subprocess.$FUNC(..., shell=True, ...)`），自动追踪数据从不可信入口到危险操作的传播路径。

**结果解读训练：** 对每条命中结果，要求学生回答五个问题——命中在哪里（文件+行号+代码片段）、规则为什么命中（匹配了哪段语法结构）、风险是什么（注入/泄露/绕过/弱加密）、是真正的问题还是误报、如何给出最小修复建议。这五个问题对应了安全分析师在审查工具输出时的标准思维流程。

**实验作业：** 学生编写自定义 Semgrep 规则检测 Python Flask Web 应用中的命令注入漏洞：

```yaml
rules:
- id: test_rule
  message: command injection
  severity: ERROR
  languages: [python]
  mode: taint
  pattern-sources:
    - pattern: request.form.get(...)
    - pattern: request.args.get(...)
  pattern-sinks:
    - pattern: subprocess.$FUNC(..., shell=True, ...)
      metavariable-regex:
        metavariable: $FUNC
        regex: "^(check_output|run|call|Popen)$"
```

扫描目标 code-repo（一个存在多处安全漏洞的 Flask 应用），成功发现两个 Blocking 级别的命令注入漏洞，分别位于文件下载接口和 Ping 诊断接口。同时编写配套测试文件（`# ruleid:` / `# ok:` 注释标记），验证规则的精确率和召回率。

技术收获包括：理解 AST 模式匹配与传统正则表达式的本质区别、掌握污点追踪的 Source-Sink 模型、学会编写可复用和可测试的安全规则、建立「自动化不是魔法，规则必须迭代」的工程认知——任何静态分析工具的规则都需要在误报和漏报之间持续调优。

---

## 五、二进制层面的软件分析与安全

### 5.1 angr 符号执行与 ReAct Agent

本模块引入 AI Agent 概念，要求学生实现基于 ReAct 范式的智能逆向分析系统。LLM 作为编排层，angr（加州大学圣塔芭芭拉分校开发的开源符号执行框架）作为可调用工具。

**符号执行原理：** 传统执行使用具体输入值运行程序一次只能遍历一条路径。符号执行用符号变量替代具体输入，在执行过程中维护路径约束条件集合，到达目标点时通过 z3 约束求解器反推满足条件的输入值——这意味着一次符号执行可以同时探索多条程序路径。

**CFG 分析与 blank_state：** angr 的 CFGFast 算法快速构建基本块拓扑结构。实验利用符号表定位 `check_password`（密码验证）和 `gadget_trap`（死循环陷阱）的地址。不使用程序入口点（会在 libc 函数中遭遇路径爆炸），而是从 `check_password` 函数入口创建 blank_state，手动设置栈帧和寄存器，将探索范围从整个程序缩小到目标函数的几十个基本块。

**ReAct 循环：** Agent 遵循 Thought → Action → Observation 的闭环范式。每轮 LLM 推理下一步应调用什么工具（Thought），Agent 执行 angr 操作（Action），系统记录工具返回的实际结果（Observation）——Observation 严格来自工具返回，不含 LLM 臆测。

**实验结果：** Agent 在 3 轮 ReAct 交互中通过符号执行求解出 crackme 密码 "AZcE"：`input[0]='A'`、`input[1]='Z'`、`input[2]=chr('q'^0x12)='c'`、`input[3]=chr('H'-3)='E'`。`gadget_trap` 死循环被 `explore(find, avoid)` 成功避开。实际运行验证了全部四条路径的正确覆盖。

思考题回答了 LLM 在符号执行中的角色定位：LLM 承担决策与编排层角色，通过语义理解指定 find/avoid 地址，将纯符号执行在 libc 函数中面临的路径爆炸问题化解为几十个基本块内的精确探索——语义引导的符号执行比纯自动探索高效数个数量级。

### 5.2 radare2 + Ghidra + ReAct Agent 静态漏洞挖掘

作为课程的综合性实验，将 ReAct Agent 推向真实安全分析场景：面对完全无源码、无符号表（已 strip）的黑盒 ELF 二进制文件，由 Agent 自主决策分析策略，调度 radare2 和 Ghidra 两类工业标准开源工具，最终输出结构化漏洞结论。

**radare2：** 开源社区最强大的命令行逆向框架。`aaa` 执行标准分析，`afl` 列出函数及其控制流，`pdf` 反汇编函数，`axt` 追踪交叉引用。命令行驱动的可编程接口使其天然适合作为被 AI 调用的后端工具。

**Ghidra：** NSA 开源的反编译器平台。反编译器将汇编指令转为 P-Code 中间表示，经数据流分析、类型推断、控制流结构恢复后输出类 C 伪代码。Headless 模式允许通过脚本驱动完整分析流程，无需 GUI。

**工具封装：** 实验封装了 5 个 LangChain 工具——r2 三件套（`r2_analyze_binary`、`r2_disassemble_function`、`r2_analyze_xrefs_and_sinks`）提供低层控制流洞察和精确地址数据；Ghidra 两件套（`ghidra_decompile_function`、`ghidra_analyze_binary`）提供高层语义理解和类型信息——两者互补形成完整的分析矩阵。

**六轮分析过程：**

Agent 通过六轮 Thought → Action → Observation 完成分析：

第一轮（r2 全局概览）：加载二进制，aaa 分析识别出 10 个函数，获取 64 位 ELF、动态链接、GCC 11.4.0、NX 开启等文件属性。

第二轮（r2 深度反汇编）：反汇编代码量最大的 main（131 字节），发现 `fgets(buf, 128, stdin)` → `strlen` 检查（≤100）→ `__strcpy_chk` 的 Source-to-Sink 数据流。注意到 `edx=0x10`（dest_size=16）与 `sub rsp, 0xa0`（实际栈帧 160 字节）之间存在 10 倍矛盾。

第三轮（Ghidra 语义确认）：反编译 main 为伪 C 代码，从语义层面确认了 `fgets` 是 Source、`__strcpy_chk(buf, input, 16)` 是 Sink。发现 `argc > 100` 分支的 `malloc(512)+free()` 是无实际效果的干扰代码。

第四轮（排除误报）：反编译辅助函数 `helper_format_print`，确认其 `__snprintf_chk` 调用正确传入 dest_size=160，安全可靠，排除怀疑。

第五轮（r2 数据流验证）：交叉引用和字符串分析确证了从 stdin 到 `__strcpy_chk` 的完整攻击面。

第六轮（Ghidra 综合确认）：全程序反编译作为最终验证，确认唯一漏洞点位于 main 的偏移 0x401382。

**漏洞结论：** 栈缓冲区溢出（stack_buffer_overflow）。核心矛盾在于——长度检查允许最多 100 字节通过，`__strcpy_chk` 被告知目标缓冲区只有 16 字节，但实际栈帧有 160 字节。用户输入超过 16 字节时，要么发生实际溢出，要么触发 `__chk_fail` 导致程序异常终止。

输出为结构化 `vuln.json`，包含三个固定字段（`vuln_type`、`location`、`cause`），完全来自 Agent Final Answer 的结构化结果，与 ReAct 日志完全一致，不包含任何手工编造的内容。Agent 支持双模式运行：工具可用时调用真实 r2/Ghidra，不可用时自动切换 demo 模式（capstone + pyelftools 预计算分析）。

---

## 六、研究专题与小组汇报

课程设置了专门的研究性专题教学环节，要求学生在以下前沿方向中选择课题进行深入研究并以小组形式汇报（占总分的 35%）：

### 6.1 研究方向概览

**代码语言模型（Code Language Models）：** 面向代码场景对大模型进行后训练与微调。代表性工作包括 EditLord（代码编辑规则学习，ICML'25）、SemCoder（融合全面语义的代码模型训练，NeurIPS'24）、TRACED（执行感知的源码预训练，ICSE'24）、CYCLE（代码生成自优化，OOPSLA'24）。

**编码智能体（Coding Agents）：** 基于 Agent 架构完成复杂软件工程任务——遗留代码迁移（C2saferrust：C→Rust 安全转换）、程序修复（SemAgent：语义感知修复 Agent）、内核崩溃修复（CrashFixer）、测试生成（FaultLine：漏洞概念验证代码自动生成）。

**评测基准（Benchmarking）：** 面向安全等场景构建评测体系——代码模型的漏洞检测能力评估（ICSE'25）、Linux 内核崩溃修复基准（Kgym，NeurIPS'24）、版本相关代码生成评测（LibEvolutionEval，NAACL'25）。

**实证研究（Empirical Evaluation）：** 从测量和分析角度审视模型在代码任务中的行为——推理能力的动态基准评测（ICML'25）、大模型幻觉的 API 文档缓解策略（ICSE'25）、代码大模型的自一致性评估（ICLR'24）。

### 6.2 方法论启示

课程通过研究专题的教学，引导学生理解学术界在「AI + 软件工程 + 安全」交叉领域的前沿工作。这些研究揭示了几个重要趋势：AI 模型正从单一的代码补全工具演化为能理解语义、执行修复、生成测试的智能化软件开发助手；安全分析从人工逐行审计向 AI 驱动自动化检测转变；但模型的可信性、一致性和幻觉问题仍是当前研究面临的重大挑战。这些认知构成了课程报告中对 AI Agent 角色定位的理论支撑。

---

## 七、课程知识体系总结

### 7.1 工具链全景

课程覆盖了开源软件安全分析的完整工具栈，所有工具均采用开源协议：

| 层次 | 工具/平台 | 教学环节 |
|------|-----------|----------|
| 协作与版本控制 | Git / GitHub | 实验一：分支合并与远程推送 |
| 包管理与分发 | PyPI / TestPyPI / twine / build | 实验二：PyPI 打包发布 |
| 静态代码分析 | Semgrep | 实验三：污点追踪规则与漏洞扫描 |
| 符号执行 | angr / z3 | 实验四：ReAct Agent 驱动符号执行 |
| 二进制反汇编 | radare2 | 实验五：黑盒 ELF 反向分析 |
| 反编译器 | Ghidra | 实验五：语义层漏洞确认 |
| AI 编排 | LangChain / LLM | 实验四、五：ReAct 闭环设计 |

### 7.2 核心方法论

贯穿课程所有实验的方法论主线是：**将专家知识编码为可复用的规则或工具，由 AI 编排层自动调度执行**。这一方法论在课程的三个层面体现：

**知识编码层：** 安全漏洞模式被编写为 Semgrep 规则；逆向分析技能被封装为 angr/r2/Ghidra 工具；协作规范体现为 Git 分支策略。每个工具都承载了特定领域的专家知识。

**编排决策层：** LLM 在 ReAct 循环中负责语义理解（从反汇编输出识别危险模式）、策略选择（决定下一步分析哪个函数）和结果综合（从分散证据提炼结构化结论）。LLM 不替代工具的直接分析能力，而是调度工具、解读结果、做出判断。

**验证闭环层：** 每个实验都设计了验证环节——Semgrep 的 test file、angr 的 exploit 验证、r2+Ghidra 的 vuln.json 与日志一致性检查。自动化分析的结果始终需要可验证的客观证据支撑。

### 7.3 递进式课程设计

课程的实验设计沿着一条精心规划的递进路径：

**模块一至二**建立了工程基础——Git 协作和 PyPI 打包是所有后续实验的基础设施，确保学生具备在开源生态中工作的基本能力。

**模块三**引入自动化工具——Semgrep 的规则驱动分析教会学生如何将安全知识编码为可执行的规则。此时分析逻辑仍需人工设计。

**模块四**将 AI 引入分析循环——angr ReAct Agent 实现了从人工操作工具到 AI 编排工具的跃迁。但分析目标较为明确（已知的密码检查逻辑）。

**模块五**释放完整能力——面对完全未知的黑盒二进制，Agent 自主选择策略、调度多种工具、排除误报、形成结论。工具也从单一框架扩展为 r2+Ghidra 工业级工具链。

每一步构建在前一步的能力之上，形成「基础设施 → 工具使用 → AI 编排 → 自主分析」的能力上升螺旋。

---

## 八、反思与展望

### 8.1 LLM 在安全分析中的定位

课程最核心的启示是对 LLM 角色的精准定位。LLM 不是安全的万能钥匙——它不直接扫描代码、不执行符号探索、不读懂二进制指令。但 LLM 提供三项不可替代的能力：

**语义理解能力：** 从反汇编输出中识别 `fgets → strlen → __strcpy_chk` 意味着「输入→检查→不安全复制」的数据流模式，这种模式识别源于 LLM 训练中获得的大量代码理解经验。

**策略决策能力：** 面对 10 个函数判断 main 是优先目标；面对反编译输出区分真正危险的 `__strcpy_chk` 和正确使用 `__snprintf_chk` 的安全函数。每轮 Thought 本质上是基于全局上下文的微决策。

**结果综合能力：** 将分散的反汇编片段和反编译伪代码提炼为「类型—位置—原因」的逻辑链条——这是从证据到结论的推理过程。

这三项能力恰好对应安全分析师的标准工作流程：理解代码、判断风险、撰写报告。课程展示了如何通过工具封装和 ReAct 循环将这些能力注入自动化系统。

### 8.2 开源工具生态的教育价值

课程坚持开源工具优先的教学选择本身具有重要的教育意义。与商业闭源产品不同，开源工具的源代码完全可审查——学生不仅学会使用 angr 的 API，还深入理解了 blank_state 的原理和约束求解的工作机制。这种对底层原理的掌握是任何使用教程无法替代的，也是开源教育的核心价值。此外，Semgrep 的规则社区、r2 的插件生态、Ghidra 的脚本扩展——为学生课后持续探索提供了开放的入口。

### 8.3 未来方向

基于课程所学，几个自然的延伸方向值得探索：一是多 Agent 协作——将 Semgrep 审计、angr 探索、r2 反汇编、Ghidra 反编译分别交由专业 Agent，由 Central Agent 统一调度，实现从源代码到二进制的全维度分析 Pipeline；二是知识沉淀与复用——将每次分析的 Thought 链和结论结构化存储为案例库，供后续分析参考，形成可成长的安全知识图谱；三是 CI/CD 集成——将完整的分析工具链嵌入持续集成流程，在每次代码提交时自动触发多层次安全验证。

---

## 九、结语

从学习 Git 分支操作和 PyPI 打包发布开始，到掌握 Semgrep 规则编写和污点追踪，再到构建能自主调度 r2 与 Ghidra 分析黑盒二进制漏洞的 AI Agent——本课程带领学生完成了一次从开源软件工程实践到 AI 驱动安全分析的完整技术旅程。

在开源组件占比持续攀升、AI 能力快速演进的今天，软件安全分析的方法论正在经历深刻变革。纯人工审计效率低、覆盖面窄；纯自动扫描误报多、缺乏上下文理解。本课程所探索的「AI 编排 + 开源工具链」的架构范式，为安全分析的智能化转型提供了清晰且可实践的路径——LLM 提供决策智能与语义理解，开源专业工具提供精确的技术执行能力。两者的结合，正在重新定义下一代安全分析的工作方式。

北京交通大学李强老师讲授的这门课程，不仅传授了具体的技术技能，更重要的是构建了一套完整的认知框架——理解开源生态的运行逻辑、掌握安全分析的核心方法论、洞察 AI 在软件工程中的角色与边界。这些知识框架将在未来的学习和研究中持续发挥作用。

---

*本报告涵盖 2026 年 3 月至 6 月「开源软件实践与安全分析」课程全部教学内容与实验环节，撰写时参考了课堂讲义 PDF（导论、开源软件生态、Git 教程、PyPI、源代码 LLM、Semgrep 教学讲义、分组报告）及全部实验作业。*
