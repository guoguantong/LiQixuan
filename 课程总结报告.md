# 开源软件实践与安全分析 — 课程总结报告

**姓名：** 李琪轩
**学号：** 25140931
**日期：** 2026年6月13日
**代码仓库：** https://github.com/guoguantong/LiQixuan

---

## 一、课程概述

「开源软件实践与安全分析」课程以开源软件生态中的核心技术与工具链为主线，系统讲授了从版本控制、静态代码分析到 AI 驱动的自动化逆向工程的完整知识体系。课程采用理论与实践深度融合的教学方式，每节讲授原理后紧接着动手实验，通过四次递进式作业，引导学生从 Git 协作开发的工程实践出发，逐步深入到基于大语言模型编排专业安全工具的智能分析系统构建。

本报告对课程全部实验内容进行系统性回顾，阐述各模块的技术原理、实践过程与学习收获。

---

## 二、模块一：Git API 与分支操作

### 2.1 实验概述

Git 是全球最大的开源协作平台的核心基础设施，也是任何开源软件工程实践的第一课。本模块从 Git 的基本操作出发，深入学习 Git 的分支模型、合并策略，以及通过 API 进行编程式仓库管理，为进一步的软件工程协作打下基础。

### 2.2 技术原理

**Git 的数据模型：** Git 本质上是一个内容寻址的文件系统。每次 commit 生成一个 SHA-1 哈希值作为唯一标识，commit 对象包含指向树对象（tree）、父提交（parent）和提交信息的引用。理解这一底层模型是掌握 Git 所有高级操作的前提。

**分支模型：** Git 的分支本质是指向某个 commit 对象的可变指针，创建分支的成本极低（仅写入一个 41 字节的引用文件），这使得 Git 的分支策略极其灵活。课程中实践了功能分支工作流：从主分支创建功能分支 → 独立开发 → 合并回主分支，这是开源社区最普遍的协作模式。

**合并策略：** 课程涵盖了两种核心合并方式。Fast-forward 合并发生在目标分支没有新提交时，仅将指针前移，保持线性历史；三路合并（3-way merge）在分支分叉时执行，生成一个合并提交。重点学习了合并冲突的产生原因和解决方法——当两个分支修改了同一文件的同一区域时，Git 无法自动决定取舍，需要人工介入。

**Git API 编程：** 在掌握命令行操作的基础上，实验进一步学习通过代码进行 Git 操作。GitHub 的 REST API v3 提供了完整的仓库管理接口，包括创建分支、提交文件、创建 Pull Request 等。学生通过 API 实现了编程式的仓库管理，理解了 CI/CD 流水线中自动化的 Git 操作原理。

### 2.3 实验实践

实验内容涵盖以下操作链：

1. **仓库初始化与远程配置：** 使用 `git init` 初始化本地仓库，通过 `git remote add` 配置远程 GitHub 仓库，理解本地与远程仓库之间的关联关系。

2. **分支创建与切换：** 实践 `git branch` 和 `git checkout -b` 创建功能分支，在隔离环境中进行代码修改，避免直接影响主分支。

3. **协同开发模拟：** 在不同分支上独立开发，分别提交修改，模拟多人协作场景下分支分叉的情况。

4. **合并与冲突解决：** 执行 `git merge` 合并分支，当出现冲突时手动编辑文件解决冲突标记（`<<<<<<<`、`=======`、`>>>>>>>`），完成合并后提交。

5. **推送到远程分支：** 学习使用 `git push origin local-branch:remote-branch` 语法将本地分支推送到远程的不同分支名，实现分支级别的仓库隔离。

6. **Git API 编程操作：** 通过 GitHub REST API 实现代码层面的仓库管理操作，为后续自动化作业中的 Git 集成（如自动推送分析结果）奠定基础。

### 2.4 学习收获

Git 是开源软件工程的第一生产力工具。通过本模块的学习，学生不仅掌握了分支模型的底层原理，理解了合并冲突的解决策略，更重要的是建立了「分支隔离、渐进集成」的工程思维方式。这一思维方式在后续所有实验中持续发挥作用——每个实验项目都使用独立分支进行版本管理，作业产出通过远程仓库统一交付。Git API 的编程能力也为后续 AI Agent 实验中自动化提交分析结果提供了技术支撑。

---

## 三、模块二：Semgrep 静态代码分析

### 3.1 实验概述

静态代码分析是开源软件安全领域最广泛使用的质量保障手段之一。本模块以 Semgrep 这一开源静态分析引擎为核心，学习如何通过规则驱动的模式匹配和污点追踪技术，在源代码层面自动发现安全漏洞。

### 3.2 Semgrep 技术原理

Semgrep 是一款基于抽象语法树（AST）的轻量级静态分析引擎。与传统正则表达式匹配不同，Semgrep 将源代码解析为 AST，在语法层面进行模式匹配，因此能够区分变量名、函数调用、字符串字面量等语法结构。其三大核心技术能力包括：

**模式匹配：** Semgrep 使用与目标语言相同语法的模式串，能精确匹配特定的代码结构。例如 `subprocess.$FUNC(...)` 可以匹配任何 `subprocess` 模块的函数调用，`$FUNC` 为元变量，捕获实际的函数名。

**污点追踪（Taint Analysis）：** 这是本次实验使用的核心功能。污点追踪通过定义 Source（不可信数据入口）和 Sink（危险操作汇聚点），自动跟踪数据从源到汇的传播路径。即使数据经过变量赋值、函数参数传递等中间步骤，只要存在从 Source 到 Sink 的路径，就能被检测到。这一机制模拟了攻击者的思维路径：寻找用户可控的输入点 → 追踪数据的传播路径 → 判断是否到达危险操作。

**跨函数分析：** Semgrep 能够跨越函数边界追踪数据流。例如，Request 参数传入 `get_host()` 函数，返回值再传入 `subprocess.call()`，在三个函数之间形成 Source → Intermediate → Sink 的链条，跨函数污点追踪能够完整捕获。

### 3.3 实验实践

本次实验的目标是编写自定义 Semgrep 规则，检测 Python Flask Web 应用中存在的命令注入漏洞。编写的规则核心逻辑如下：

**Source 定义：** Flask 框架中，HTTP 请求参数通过 `request.form.get()` 或 `request.args.get()` 获取，这些参数完全由用户控制，是不可信数据的入口。

**Sink 定义：** Python 的 `subprocess` 模块在 `shell=True` 模式下执行命令时，如果命令字符串中拼接了外部输入，就构成命令注入。使用元变量正则表达式 `^(check_output|run|call|Popen)$` 精确匹配四个关键的危险函数。

**规则效果：** 当且仅当存在从 HTTP 请求参数到 `shell=True` 子进程调用的数据流时触发告警。

同时编写了配套的测试文件，包含明确的 `# ruleid:` 和 `# ok:` 注释标记正例与反例，用于验证规则的准确率（precision）和召回率（recall）。

### 3.4 实验成果

对目标 Flask 应用 code-repo 进行扫描，成功发现 2 个真实的命令注入漏洞：

**漏洞一（文件下载接口）：** `subprocess.call(["sh", "-c", "cat " + filepath], shell=True)` —— `filepath` 参数来自 URL 查询字符串，通过 `os.path.join` 处理后直接拼接到 shell 命令中。攻击者可构造包含 `; rm -rf /` 等恶意命令的文件名参数，实现命令注入与路径遍历的叠加攻击。

**漏洞二（Ping 诊断接口）：** `subprocess.check_output("ping -c 1 " + host, shell=True)` —— `host` 参数来自 POST 表单，直接拼接入 shell 命令。攻击者输入 `127.0.0.1; cat /etc/passwd` 即可执行任意系统命令。

两个漏洞均被 Semgrep 标记为 Blocking 级别，实验输出清晰显示了漏洞所在的文件、行号和匹配的代码片段。

### 3.5 学习收获

通过本模块，学生理解了静态代码分析的核心范式——将安全知识编码为可复用的规则。一条精心设计的 Semgrep 规则可以自动化地重复审计成千上万行代码，其效率和一致性远超人工审查。同时，编写规则的过程也是系统化梳理漏洞模式的过程：分析典型的攻击面、追踪数据的传播路径、定义精确的检测条件——这种结构化的安全思维是开源软件安全保障的基础能力。

---

## 四、模块三：ReAct Agent + angr 符号执行

### 4.1 实验概述

本模块将课程引入 AI Agent 领域，要求学生实现基于 ReAct 范式的智能分析系统。Agent 使用 LLM 作为编排层，angr（开源符号执行框架）作为可调用工具，对二进制 crackme 程序进行自动化逆向分析，通过符号执行求解正确密码。

### 4.2 符号执行技术原理

angr 是加州大学圣塔芭芭拉分校开发的开源二进制分析框架，其符号执行引擎是实验的核心技术。

**符号执行的核心思想：** 传统执行使用具体的输入值运行程序一次遍历单条路径；符号执行用符号变量替代具体输入，在执行过程中维护约束条件集合（路径约束），到达目标点时通过 z3 等约束求解器反推满足条件的输入值。这意味着一次符号执行可以同时探索多条程序路径。

**CFGFast 分析：** angr 的控制流图快速恢复算法能够跳过间接跳转的精确解析，快速构建程序的基本块拓扑结构。实验中利用符号表直接定位 `check_password` 和 `gadget_trap` 的地址，进行针对性分析。

**blank_state 技术：** 不从程序入口点开始执行（这会在 libc 的 `strlen`、`scanf` 等函数中遭遇路径爆炸），而是创建 blank_state 直接从目标函数入口开始，手动设置栈帧和寄存器，将探索范围从整个程序缩小到目标函数的几十个基本块。

**explore 方法：** `SimulationManager.explore(find, avoid)` 接收目标地址集合和避开地址集合，自动引导探索器。实验中 `find` 指向 Success 路径，`avoid` 指向 `gadget_trap` 死循环入口。

### 4.3 ReAct Agent 架构

ReAct 范式将推理（Reasoning）与行动（Acting）交替进行：

```
Thought → Action → Observation → Thought → Action → Observation → ... → Final Answer
```

**Thought（思考）：** LLM 根据当前状态和目标推理下一步应调用的工具及参数。例如，第一轮推理出应先加载二进制获取函数列表，第二轮根据函数列表决定反汇编 `check_password`。

**Action（行动）：** Agent 调用 LangChain Tool Calling 封装的 angr 工具执行具体操作。每次调用返回结构化结果。

**Observation（观察）：** 严格来自工具的实际返回值，不包含 LLM 的臆测或编造。这是 ReAct 范式与传统 LLM 自由生成回答的本质区别——Agent 的判断必须建立在工具验证过的客观事实上。

### 4.4 工具设计

实验封装了三个 angr 工具：

| 工具 | 功能 | 核心 API |
|------|------|----------|
| `analyze_binary` | 加载 ELF，符号表定位关键函数 | `angr.Project()`, `loader.find_symbol()` |
| `locate_success_address` | 反汇编 check_password，定位 find/avoid 地址 | `CFGFast()`, `factory.block()`, capstone |
| `symbolic_explore_and_extract` | 符号执行 + 约束求解提取密码 | `simgr.explore(find, avoid)`, `solver.eval()` |

工具之间的数据通过 SharedState 对象传递：analyze_binary 定位函数地址存储到 state，locate_success_address 读取地址并补充 find/avoid 地址，symbolic_explore_and_extract 使用所有前置信息执行探索。

### 4.5 实验成果

Agent 在 3 轮 ReAct 交互中成功求解出 crackme 的正确密码 "AZcE"：

- `input[0] = 'A'`（直接等值约束）
- `input[1] = 'Z'`（通过 if 条件约束，且需避开 `input[1]=='B'` 的陷阱路径）
- `input[2] = chr(ord('q') ^ 0x12) = 'c'`（XOR 约束求解）
- `input[3] = chr(ord('H') - 3) = 'E'`（加法约束求解）

实际运行 crackme 验证了密码正确性，并覆盖测试了全部四条路径：Success（AZcE）、Trap/死循环（ABxx）、Wrong password（XXXX）、长度不足（AZ）。

### 4.6 学习收获

本模块是课程的重要转折点，学生首次接触 AI Agent 的设计与实现。核心技术收获包括：理解符号执行如何通过约束求解精确求解数学约束、掌握 LangChain Tool Calling 协议用于将任意 Python 函数暴露为 LLM 可调用的工具、理解 ReAct 循环中 Observation 必须来自工具返回的设计原则。更深层的启示在于 LLM 在自动化分析中的角色——它不是直接进行分析，而是理解工具的输出、识别关键信号、排除误导信息、做出策略决策。这种「AI 编排 + 工具执行」的架构是本课程后续实验的核心范式。

---

## 五、模块四：ReAct Agent + r2 + Ghidra 二进制静态分析

### 5.1 实验概述

作为课程的综合性实验，本模块将 ReAct Agent 范式推向真实的安全分析场景。与模块三不同——模块三求解的是一个已知存在密码检查逻辑的 crackme，模块四面对的是完全无源码、无符号表的黑盒 ELF 二进制文件。分析目标从求解特定值转变为发现未知漏洞，工具从 angr 替换为 radare2 和 Ghidra 这两个逆向工程领域的工业标准开源工具。

### 5.2 radare2 技术原理

radare2（简称 r2）是开源社区最强大的命令行逆向工程框架之一。与带有 GUI 界面的工具不同，r2 的设计哲学是纯命令行驱动的可编程分析，非常适合作为被 AI Agent 调用的后端工具。

**分析深度分级：** r2 提供 `-aa`（快速分析）、`-aaa`（标准分析）、`-aaaa`（深度分析）等多级分析命令。实验中选用 `-aaa`，在分析深度与执行时间之间取得平衡。

**控制流分析：** `afl` 命令列出所有已识别的函数及其地址、大小和跳转信息。在已 strip 无符号表的情况下，r2 通过函数序言模式（如 `push rbp; mov rbp, rsp; sub rsp, N`）和 `call` 指令的目标地址自动识别函数边界。

**交叉引用分析：** `axt` 命令追踪符号的交叉引用，即哪些位置的代码引用了该符号。实验中通过交叉引用确认 `__strcpy_chk` 被 main 函数调用，建立调用链。

### 5.3 Ghidra 技术原理

Ghidra 是美国国家安全局开源的反编译器平台，其核心优势在于将汇编代码提升为类 C 伪代码的反编译能力。

**反编译流程：** Ghidra 的反编译器首先将汇编指令转换为 P-Code（中间表示），然后通过数据流分析、类型推断、控制流结构恢复等一系列变换，最终生成可读的类 C 伪代码。这一流程本质上是对编译过程的逆向。

**Headless 模式：** Ghidra 默认以图形化界面运行，但提供 Headless 批处理模式，允许通过命令行和 Java/Python 脚本驱动完整分析流程。实验中使用 Python 脚本来调用 `DecompInterface.decompileFunction()` API，将反编译结果捕获为文本供 Agent 分析。

**完全无符号表的应对：** 在已 strip 的二进制上，Ghidra 通过启发式函数识别（检测函数序言模式）和调用约定推断来恢复函数边界和参数信息。虽然函数名只能显示为 `func_0x401264` 之类的地址标记，但反编译输出的伪代码仍然可以清晰展示程序逻辑和数据流。

### 5.4 工具封装设计

实验将 r2 和 Ghidra 封装为 5 个 LangChain 工具：

**r2 三件套：**

| 工具 | 功能 | 核心命令 |
|------|------|----------|
| `r2_analyze_binary` | 加载二进制，执行 aaa 分析，列出函数及入口点 | `ie`, `ii`, `aaa`, `afl`, `pdf @ entry0` |
| `r2_disassemble_function` | 反汇编指定函数，搜索危险调用 | `pdf @ addr`, `/c strcpy\|gets\|...` |
| `r2_analyze_xrefs_and_sinks` | 交叉引用分析与危险 Sink 追踪 | `axt`, `izz` |

**Ghidra 两件套：**

| 工具 | 功能 | 核心 API |
|------|------|----------|
| `ghidra_decompile_function` | 反编译指定函数为伪 C 代码 | `DecompInterface.decompileFunction()` |
| `ghidra_analyze_binary` | 全程序反编译 + 漏洞标记 | 全函数遍历反编译 + 模式匹配 |

### 5.5 六轮 ReAct 分析全记录

Agent 通过六轮完整的 Thought → Action → Observation 循环完成分析：

**第 1 轮（r2 全局概览）：** 加载二进制，执行 aaa 分析，获取文件信息（64 位 ELF、动态链接、由 GCC 11.4.0 编译、已 strip、无 canary、NX 开启）和 10 个函数的列表。入口点分析确认了 `__libc_start_main` 调用 main（地址 0x401264）。

**第 2 轮（r2 深度反汇编）：** 反汇编代码量最大（131 字节）的 main 函数。在反汇编输出中发现了关键数据流模式：`fgets(buf, 128, stdin)` 读取输入 → `strcspn` 去换行 → `strlen` 长度检查（≤100）→ `__strcpy_chk` 复制到栈。特别注意 `mov edx, 0x10` 将 dest_size 设为 16，而此行之前的 `sub rsp, 0xa0` 分配了 160 字节栈帧——两数之间存在 10 倍差异。

**第 3 轮（Ghidra 语义确认）：** 反编译 main 函数，从伪 C 代码角度确认：`fgets(input, 128, stdin)` 是 Source，`__strcpy_chk(buf, input, 16)` 是 Sink。`buf` 实际有 160 字节但 `__strcpy_chk` 被告知只有 16 字节。还发现 `argc > 100` 分支中的 `malloc(512)` + `free()` 是一段无实际效果的代码，可能是故意设置的干扰项。

**第 4 轮（Ghidra 排除误报）：** 反编译辅助函数 `helper_format_print`（0x401216），确认它使用 `__snprintf_chk` 并正确传入 dest_size = 160——这个函数是安全的，仅用于打印 `[boot] profile-service ready` 和 `[selftest] selftest-payload-ok` 启动消息。排除了对其他函数的怀疑，锁定 main 为唯一可疑目标。

**第 5 轮（r2 数据流验证）：** 交叉引用分析确认了 Source-to-Sink 链条的每个环节。字符串提取显示程序包含 `[%s] %s`、`profile-service ready`、`boot`、`selftest`、`selftest-payload-ok`——一个名为 profile-service 的服务程序，这正是它被命名为 "challenge" 的原因。

**第 6 轮（Ghidra 综合确认）：** 对全程序进行 Ghidra 完整分析，反编译所有函数，生成安全发现摘要。

### 5.6 漏洞结论

结构化输出 `vuln.json` 包含三个固定字段：

```json
{
  "vuln_type": "stack_buffer_overflow",
  "location": "main (0x401382): __strcpy_chk call",
  "cause": "不可信的用户输入由 fgets 从 stdin 读入（最多 128 字节），
           经过 strlen 长度检查（允许 ≤100 字节），
           随后由 __strcpy_chk 复制到栈缓冲区，但 dest_size 参数仅为 16 字节，
           与实际栈帧 160 字节严重不匹配，导致输入超过 16 字节时发生栈缓冲区溢出"
}
```

核心矛盾在于参数不匹配：长度检查允许 100 字节通过，`__strcpy_chk` 声称 16 字节，但实际有 160 字节。这一不匹配意味着用户输入超过 16 字节（但不超过 100 字节）时，要么绕过保护机制发生溢出，要么触发 `__chk_fail` 导致程序异常终止。

### 5.7 可移植性设计

Agent 设计了双模式运行架构：工具可用时通过 `subprocess` 调用真实 r2 和 Ghidra，Observation 完全来自工具的实际输出；工具不可用时自动切换 demo 模式，使用 capstone + pyelftools 预计算分析结果展示完整 ReAct 过程。这保证了代码在任何环境下均可一键运行验证，不因缺少特定依赖而不可执行。

### 5.8 学习收获

模块四将学生从工具使用者提升为系统构建者。核心技术收获包括：理解 r2 和 Ghidra Headless 的可编程接口和输出解析、掌握如何在已 strip 的无符号二进制上通过汇编模式识别函数和数据流、领会 ReAct 循环中多工具协作的设计策略——r2 提供低层控制流洞察和精确的地址数据、Ghidra 提供高层语义理解和类型信息，两者互补形成完整的分析能力。

---

## 六、技术体系总结

### 6.1 工具链全景

四次实验覆盖了开源软件安全分析的完整工具栈：

| 层次 | 工具 | 开源协议 | 应用场景 |
|------|------|----------|----------|
| 版本控制 | Git / GitHub API | GPL-2.0 | 代码管理与协作 |
| 静态分析 | Semgrep | LGPL-2.1 | 源代码模式匹配与污点追踪 |
| 二进制分析 | radare2 | LGPL-3.0 | 汇编级反汇编与控制流分析 |
| 反编译 | Ghidra | Apache-2.0 | 二进制到伪 C 代码语义还原 |
| 符号执行 | angr / z3 | BSD-2 / MIT | 路径探索与约束求解 |
| AI 编排 | LangChain / LLM | MIT | Tool Calling 与 ReAct 循环 |

这些工具的共同特点是都遵循开源协议，都是其所在领域社区广泛采纳的工业标准工具。课程选择这些工具并非随机，而是精心构建了一条从代码提交到安全验证的完整开源工具链。

### 6.2 核心方法论

贯穿四次实验的方法论主线是：**将人类专家的分析知识编码为工具或规则，由 AI 编排层自动调度执行**。

在模块一中，协作规范被编码为 Git 的分支策略和 API 调用——每步操作都有明确的输入输出。

在模块二中，漏洞模式被编码为 Semgrep 规则——Source 到 Sink 的路径是精确定义的匹配模板。

在模块三和四中，逆向分析策略被编码为 ReAct Agent 的 Tool 集合和 System Prompt——LLM 在每个 Thought 步骤中决定下一步调用什么工具、传递什么参数、如何解读返回结果。

这一方法论的本质是知识工程：将专业领域知识从人脑中提取出来，编码为机器可执行的形式，再借助 LLM 的语义理解和推理能力实现灵活的编排调度。这不仅提升了分析效率，更使得安全知识具备了可复用、可传递、可审计的特性。

### 6.3 LLM 在分析链中的定位

课程中最具启发性的收获是对 LLM 角色的准确定位。LLM 并非分析的主体——它不直接扫描代码，不能执行符号探索，也看不懂二进制指令。但 LLM 提供了三项关键能力，使其成为理想的编排者：

**语义理解：** LLM 能从 r2 的反汇编输出中理解 `fgets` → `strlen` → `__strcpy_chk` 的模式意味着「用户输入 → 长度检查 → 不安全复制」的数据流，这种模式识别能力源于其训练数据中蕴含的大量代码理解经验。

**策略决策：** 面对 10 个函数，LLM 能判断代码量最大的 main 函数是应优先分析的目标；面对反编译伪代码，能区分真正危险的 `__strcpy_chk` 和正确使用 `__snprintf_chk` 的安全函数。每一轮的 Thought 本质上是一个微决策——下一步分析哪个函数、调用哪个工具。

**结果综合：** LLM 能将六轮的工具输出综合为结构化的漏洞结论，填充固定字段的 JSON。这不是简单的摘抄，而是需要从分散的汇编片段和反编译结果中提炼出「漏洞类型—位置—原因」的逻辑链条。

这三种能力——理解、决策、综合——恰好对应了人类安全专家在分析过程中的思维活动。课程展示了如何通过工具封装和 ReAct 循环，将这些能力注入到自动化的分析系统中。

---

## 七、课程反思

### 7.1 递进式课程设计

四次实验的设计不是孤立的，而是沿着一条清晰的递进路径展开：

**模块一**建立了工程基础（Git 协作），这是所有后续实验的共同基础设施。

**模块二**引入了自动化工具（Semgrep），但此时工具的分析逻辑仍是人工编码的（规则由人编写）。

**模块三**将 AI 引入分析循环，实现了从「人工操作工具」到「AI 操作工具」的跃迁。但分析目标相对明确（求解已知的密码检查逻辑），angr 工具的设计也是直接针对这个目标的。

**模块四**则是能力的释放——面对完全未知的黑盒二进制，AI Agent 需要自主选择分析策略、识别可疑模式、排除干扰信息、综合形成结论。工具也从单一框架扩展为工业级工具链。

每一步都建立在前一步的能力基础之上，这种「脚手架式」的实验设计使得学习曲线平滑上升，每次挑战都是可完成且有成就感的。

### 7.2 开源工具生态的价值

课程选择工具时坚持开源优先原则，这本身就是一个重要的教育选择。与闭源的商业安全产品不同，开源工具的源代码完全可审查，学生不仅可以理解工具的使用方法，还可以深入理解其内部实现——正如在模块三中不仅调用了 angr 的 API，还学习了 `blank_state`、`SimulationManager`、约束求解的工作原理。这种对底层原理的理解是「使用教程」无法提供的，也是开源教育的核心价值。

此外，开源工具的社区生态为学生提供了持续学习的入口。Semgrep 的规则仓库、r2 的插件系统、Ghidra 的脚本扩展——课程结束后，学生可以在这些社区中继续探索和贡献。

### 7.3 扩展方向

基于课程所学，有几个自然的延伸方向：

**多 Agent 协作：** 将 Semgrep 审计、angr 探索、r2 反汇编、Ghidra 反编译分别交由独立的专业 Agent，由 Central Agent 协调调度，实现覆盖源代码到二进制的全维度分析。

**知识沉淀与复用：** 将每次分析中 LLM 的 Thought 和最终结论结构化存储为案例库，供后续分析参考，形成可成长的安全知识图谱。

**工具链整合：** 将 Git、Semgrep、angr、r2、Ghidra 整合为统一的 CI/CD Pipeline，在每次代码提交时自动触发多层次安全分析——Git 管理版本、Semgrep 扫描源代码、angr 深度分析关键二进制、Ghidra 反编译高风险函数。

---

## 八、结语

从学习 Git 分支操作到构建能自主分析未知二进制的 AI Agent，本课程带领学生完成了一次从开源软件工程实践到智能化安全分析的技术跃迁。每次实验都在前一次的基础上推进，每次推进都带来新的认知——不仅是技术层面的「怎么做」，更是方法论层面的「为什么这样做」。

在开源软件渗透各行各业的今天，掌握开源工具的使用能力、理解工具背后的技术原理、具备将工具链与 AI 编排集成的系统设计能力，已成为软件工程师和安全分析师的核心素养。本课程所构建的「AI + 开源工具 + 安全分析」三位一体的知识框架，将继续指导未来的学习和实践。

---

*本报告涵盖 2026 年 3 月至 6 月期间「开源软件实践与安全分析」课程全部实验内容。*
